{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"additionalProperties": false,
	"definitions": {
		".ItemUPCDesc": {
			"additionalProperties": false,
			"description": "Description is not available.",
			"properties": {
				"format_id": {
					"$ref": "#/definitions/varchar21",
					"description": "This field will hold the format id that corresponds to the item's variable UPC. This value is only used for items with variable UPCs."
				},
				"item_id": {
					"$ref": "#/definitions/varchar225",
					"description": "Unique identifier for the item."
				},
				"item_number_type": {
					"$ref": "#/definitions/varchar26",
					"description": "This type can hold a number of digits count umber_type."
				},
				"item_parent": {
					"$ref": "#/definitions/varchar225",
					"description": "Unique identifier for the item/group at the level above the item."
				},
				"prefix": {
					"$ref": "#/definitions/number2",
					"description": "This field holds the prefix for variable weight UPCs. The prefix determines the format of the eventual UPC and will be used to decode variable weight UPCs that are uploaded from the POS."
				},
				"primary_ref_item_ind": {
					"$ref": "#/definitions/varchar21",
					"description": "Indicates if the sub-transaction level item is designated as the primary sub-transaction level item. For transaction level items and above, the value in this field will be 'N'o."
				}
			},
			"required": [
				"item_id"
			],
			"type": "object"
		},
		"number2": {
			"description": "This type can hold a number of digits count 2.",
			"maximum": 2147483647,
			"minimum": -2147483648,
			"type": "integer"
		},
		"varchar21": {
			"description": "This type can hold a string of max length of 1 characters.",
			"maxLength": 1,
			"type": "string"
		},
		"varchar225": {
			"description": "This type can hold a string of max length of 25 characters.",
			"maxLength": 25,
			"type": "string"
		},
		"varchar26": {
			"description": "This type can hold a string of max length of 6 characters.",
			"maxLength": 6,
			"type": "string"
		}
	},
	"description": "JSON Schema generated by XMLSpy v2021 rel. 2 sp1 (x64) (http://www.altova.com)",
	"properties": {
		
		"ItemUPCDesc": {
			"$ref": "#/definitions/.ItemUPCDesc"
		}
	},
	"type": "object"
}